name: Full build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel
    steps:
    - name: Install dependencies
      run: |
        pacman-key --init
        pacman -Syu --noconfirm
        pacman -S --noconfirm --needed nodejs git clang cmake ninja shaderc vulkan-validation-layers vulkan-utility-libraries wayland wayland-protocols libxkbcommon glm libpng libjpeg-turbo libjxl libwebp libheif systemd-libs libdrm libdisplay-info mesa python mold libtiff libraw openexr libsixel cairo librsvg libexif
    - uses: actions/checkout@v4
    - run: cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=Release
      env:
        CC: clang
        CXX: clang++
    - run: ninja -C build install
      env:
        DESTDIR: ${{ github.workspace }}/install
    - run: strip ${{ github.workspace }}/install/*
    - uses: actions/upload-artifact@v3
      with:
        path: install
