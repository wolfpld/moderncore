{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": ["-ds"],
            "cwd": "${config:cmake.buildDirectory}",
            "terminal": "console"
        }
    ]
}